## Instructions:

1. Install Gulp and Bower
	
	npm install -g gulp
	npm install -g bower

2. Install node modules:
	
	npm install

3. Install bower dependencies:

	bower install

4. Run gulp

	gulp serve